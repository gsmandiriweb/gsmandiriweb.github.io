# build output
dist/
# astro generated types
.astro/
# loader cache (fetched github posts; not committed)
.cache/
# deps
node_modules/
# local secrets — NEVER commit a real PAT
.env
.env.*
# logs
*.log
